# Backend environment variables for Docker deployment
NODE_ENV=production
DATABASE_URL=file:/app/data/stories.db
PORT=3001

# Add your API keys here (optional - can be set by users in the UI)
# ANTHROPIC_API_KEY=your_key_here
# OPENROUTER_API_KEY=your_key_here