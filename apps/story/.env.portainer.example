# Portainer Stack Environment Variables
# Copy this to .env in Portainer or set as stack environment variables

# Domain Configuration
DOMAIN=story.yourdomain.com
API_DOMAIN=api.story.yourdomain.com

# Backend Configuration
CORS_ORIGIN=https://story.yourdomain.com
NODE_ENV=production

# API Keys (Optional - users can also set in UI)
ANTHROPIC_API_KEY=your_anthropic_key_here
OPENROUTER_API_KEY=your_openrouter_key_here

# Database Path on Host
# Adjust this to where you want to store the SQLite database
DATA_PATH=/srv/docker/story/data

# Optional: Basic Auth for API (generate hash with: caddy hash-password)
# API_USERNAME=admin
# API_PASSWORD_HASH=$2a$14$...